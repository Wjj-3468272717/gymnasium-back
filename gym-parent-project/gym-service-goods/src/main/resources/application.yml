nacos:
  config:
    server-addr: 127.0.0.1:8848

spring:
  application:
    name: gym-service-goods

server:
  port: 8084

dubbo:
  application:
    name: gym-service-goods
  registry:
    address: nacos://127.0.0.1:8848
  protocol:
    name: dubbo
    host: 127.0.0.1
    port: 20884
  scan:
    base-packages: com.v1.service.goods.provider
